# ── Productivity Framework Tracker — Environment Variables ────────────────
# Copy this file to .env and fill in production values.
# NEVER commit the actual .env file to version control.

# Flask
FLASK_ENV=production
FLASK_SECRET_KEY=          # Generate: python -c "import secrets; print(secrets.token_hex(32))"

# Server
PORT=80
WEB_CONCURRENCY=4          # Number of gunicorn workers

# Database — set DATABASE_URL for PostgreSQL, or DATABASE_PATH for SQLite
# Only one is needed. DATABASE_URL takes precedence if both are set.
DATABASE_URL=postgresql://productivity:changeme@postgres:5432/productivity
# DATABASE_PATH=/data/productivity.db    # SQLite fallback (local dev)

# PostgreSQL (used by docker-compose)
POSTGRES_USER=productivity
POSTGRES_PASSWORD=changeme
POSTGRES_DB=productivity

# Logging
LOG_LEVEL=INFO             # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_DIR=/data/logs         # Where to write log files
